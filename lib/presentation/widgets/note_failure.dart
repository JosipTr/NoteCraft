import 'package:flutter/material.dart';

class NoteFailure extends StatelessWidget {
  const NoteFailure({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Database Failure"),
    );
  }
}
